リサーチアーカイブ運用規約
このリポジトリは、定期ルーティンによる調査結果を蓄積し、 GitHub上で過去分を参照できるようにするためのものである。 すべての調査系ルーティンは、以下の規約に従うこと。


0. 大原則
チャット上に結果を出力しただけで終了してはならない。 ファイルとしてコミットされて初めてタスク完了とする。
検証できなかった情報は書かない。件数を満たすための推測・水増しは禁止。
失敗を成功として報告しない。詰まったら、詰まった箇所を明示して止まる。


1. 事前確認
作業開始時に必ず実行する。

TZ=Asia/Tokyo date        # 以降すべてJSTで判断する

git pull --rebase         # 他ルーティンとの競合回避


2. 保存先
{category} は各ルーティンのプロンプトで指定される。

種別
パス
本文
docs/{category}/{YYYY}/{YYYY-MM-DD}.md
索引
docs/{category}/index.json
一覧
docs/{category}/README.md


ディレクトリが存在しない場合は作成する。


3. 本文ファイルの書式
先頭に以下のfront matterを必ず付ける（順序も固定）。

---

date: YYYY-MM-DD

category: <カテゴリ名>

title: <日本語40字以内>

tags: [<3〜5個>]

summary: <日本語100字以内。索引一覧に表示される>

lang: [ja]          # 英語本文を含む場合は [ja, en]

---

本文はMarkdown。GitHub上で読まれる前提で、見出しは##から始める。
外部リンクは必ず実在確認済みのURLのみ。未確認なら書かない。
英語の読み上げ対象テキストを含む場合、その部分を <blockquote lang="en">...</blockquote> で囲む。 後から音声化する際の抽出対象になるため、この囲みを省略しない。


4. index.json の更新
破損すると閲覧側が全滅するため、以下の手順を厳守する。

既存の index.json を読み、JSONとしてパースできることを確認する。 パースに失敗した場合は書き換えず、中断してエラーを報告する。 （ファイルが存在しない場合のみ、[] で新規作成してよい）
配列の先頭に1件追加する。

{

  "date": "YYYY-MM-DD",

  "title": "...",

  "summary": "...",

  "tags": ["...", "..."],

  "path": "YYYY/YYYY-MM-DD.md",

  "count": 0

}

書き込み後、再度パースして検証する。 失敗した場合は書き込み前の状態に戻し、報告して中断する。

同じ日付のエントリが既に存在する場合は、重複追加せず既存エントリを更新する。 本文ファイルも追記ではなく上書きする。


5. README.md の更新
docs/{category}/README.md の一覧表に、最新が上に来るよう1行追加する。 GitHubアプリで最初に開く画面になるため、リンクは相対パスで書く。

| 日付 | タイトル | 件数 |

|---|---|---|

| [YYYY-MM-DD](YYYY/YYYY-MM-DD.md) | ... | N |


6. 該当なしの場合
条件を満たす情報が1件も得られなかった場合：

空の本文ファイルを作成しない
index.json / README.md も更新しない
「該当なし」とその理由を報告して終了する


7. コミットとプッシュ
git add docs/

git commit -m "archive({category}): YYYY-MM-DD"

git push

push が失敗した場合は、原因を明示して報告する。 リトライは git pull --rebase の後に1回まで。それでも失敗したら中断する。
コミットしていない状態で「完了しました」と報告してはならない。


8. 通知
各ルーティンのプロンプトで通知手段が指定されている場合のみ実行する。 通知本文には全文を貼らず、以下を含める。

冒頭に3〜5行の要約（スマホの通知プレビューで内容が分かる程度）
GitHub上の該当ファイルへのリンク
件数


9. 完了報告の形式
報告は以下の3点のみとする。本文の再掲は不要。

保存先: docs/{category}/{YYYY}/{YYYY-MM-DD}.md

件数: N件

コミット: <ハッシュ>


## 10. 状態ファイル（前回実行の記憶）

ルーティンが前回実行の情報を引き継ぐ必要がある場合、状態ファイルは
必ず `docs/{category}/state.json` に置く。

**ホームディレクトリ（`~/`）や /tmp に状態を保存してはならない。**
実行環境はセッションごとに破棄されるため、次回実行時に必ず失われる。

- 読み込み時、JSONとしてパースできない場合は初期値で作り直し、
  「状態ファイルが破損していたため初期化した」と報告する。
- 書き込み後、再度パースして検証する。
- 状態ファイルも `git add docs/` の対象に含まれるため、
  本文と同じコミットに含めること。
- 配列が際限なく増える項目（参照済みURL等）は、
  直近6ヶ月分または1000件を上限とし、古いものから削除する。

## 美術展ルーティン：重複チェックのルール
1. 実行開始時に history/art-museum/ 配下のファイルを確認する。ファイル名（日付）でソートし、直近4件（＝直近4週間分）を読む。ファイルが1つも無い、またはディレクトリが存在しない場合（初回実行）は重複チェックをスキップして通常通り進める。
2. 読み込んだファイルの「## 掲載した展覧会」欄から「展覧会名 + 会場」の組み合わせを重複候補として抽出する。「## 今後の注目」欄は重複判定に使わない。
3. 今回のドラフトと照合し、会期が変わっておらず単純な再掲になるものは除外する。ただし「会期終了まで14日以内」の場合は再掲してよい。その際は本文に「※直近○月○日号でも紹介済み・終了間近のため再掲」と明記する。
4. 配信内容が確定したら、history/art-museum/YYYY-MM-DD.md を新規作成する（追記ではなく毎回新規ファイル）。フォーマットは以下：

# YYYY-MM-DD 配信履歴

## 掲載した展覧会
- 展覧会名: ○○展 | 会場: △△美術館 | 会期: YYYY-MM-DD〜YYYY-MM-DD

## 今後の注目として言及した展覧会（正式掲載ではない・重複対象外）
- 展覧会名: ××回顧展 | 会場: □□美術館 | 会期: YYYY-MM-DD〜

このステップは必ず配信の最後に行う（先に書くと、この回の内容が確定する前に履歴が汚れるため）。
5. history/art-museum/ 配下のファイルが9件を超えたら、古いものから history/art-museum/archive/ に移動する。
